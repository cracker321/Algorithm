# [ 이진 탐색 binary search]
# https://www.youtube.com/watch?v=1vZijMmhGNw&list=PLFgS-xIWwNVX-zm4m6suWC9d7Ua9z7fuT&index=23&ab_channel=%ED%95%98%EB%A3%A8%EC%BD%94%EB%94%A9
# https://www.youtube.com/watch?v=txWMybyeIRw&list=PLFgS-xIWwNVU_qgeg7wz_aMCk22YppiC6&index=15&ab_channel=%ED%95%98%EB%A3%A8%EC%BD%94%EB%94%A9


#============================================================================================================================

# [ 백준 1920번 ]

'''
문제
: N개의 정수 A[1], A[2], …, A[N]이 주어져 있을 때, 이 안에 X라는 정수가 존재하는지 알아내는 프로그램을 작성하시오.

입력
:첫째 줄에 자연수 N(1 ≤ N ≤ 100,000)이 주어진다. 다음 줄에는 N개의 정수 A[1], A[2], …, A[N]이 주어진다. 
다음 줄에는 M(1 ≤ M ≤ 100,000)이 주어진다. 
다음 줄에는 M개의 수들이 주어지는데, 이 수들이 A안에 존재하는지 알아내면 된다. 모든 정수의 범위는 -231 보다 크거나 같고 231보다 작다.

출력
: M개의 줄에 답을 출력한다. 존재하면 1을, 존재하지 않으면 0을 출력한다.
'''

# 즉, 여기에서 사용된 map() 기능의 핵심은, '최초 입력된 문자열 원소들 리스트'를 변환시켜서 '정수형 원소들 리스트'로 탈바꿈시키는 것이다!


N = int(input()) # 최초 입력되는 '숫자'는 당연히 '정수형'이 아니라 '문자열'로 입력된다!
A = list(map(int, input().split())) # 여기에 입력되는 숫자 N개는, 'map()'으로 인해 이제 '문자열 원소들 리스트'가 아닌 '정수형 원소 리스트'로 변환된다!

M = int(input())
B = list(map(int, input().split()))


for i in B:
    if i in A:
        print(1)
    else:
        print(0)












#============================================================================================================================



N = int(input())
A = list(map(int, input().split()))
M = int(input())
X = list(map(int, input().split()))


for i in X:
    if i in A:
        print(1)
    else:
        print(0)


#============================================================================================================================

'''

# 설명1
이 문제는 주어진 N개의 정수 목록에서 특정한 수 X가 있는지를 찾는 프로그램을 작성하는 것입니다.

우선, 입력을 받습니다.

첫 번째 줄에서는 정수 N을 입력받습니다. 이 값은 주어진 정수의 개수입니다.
두 번째 줄에서는 N개의 정수 A[1], A[2], ..., A[N]을 입력받습니다. 이 값들은 주어진 정수 목록입니다.
세 번째 줄에서는 정수 M을 입력받습니다. 이 값은 찾아야 할 수의 개수입니다.
네 번째 줄에서는 M개의 수들을 입력받습니다. 이 값들은 목록 A 안에 존재하는지 확인해야 할 수들입니다.
이제 주어진 M개의 수들이 목록 A 안에 있는지를 확인합니다.

M개의 수들을 차례대로 확인합니다.
각각의 수에 대해서, 목록 A를 처음부터 끝까지 순회하면서 해당 수를 찾습니다.
만약 해당 수를 찾으면, 그 수가 목록 A 안에 존재한다는 것을 의미하므로 1을 출력합니다.
만약 해당 수를 찾지 못하면, 그 수가 목록 A 안에 존재하지 않는다는 것을 의미하므로 0을 출력합니다.
모든 수에 대한 확인이 끝나면, 출력합니다.

위의 과정을 통해 얻은 결과를 차례대로 출력합니다.
존재하는 경우에는 1을, 존재하지 않는 경우에는 0을 출력합니다.

#================================================================


# 설명2

# 입력 받기

N = int(input())
A = list(map(int, input().split()))
M = int(input())
X = list(map(int, input().split()))

첫 번째 줄은 N = int(input())으로, 입력으로 주어지는 정수의 개수를 읽어와 변수 N에 저장합니다.
두 번째 줄은 A = list(map(int, input().split()))으로, N개의 정수를 입력받아 리스트 A에 저장합니다. input().split()은 입력을 공백을 기준으로 분리하고, map(int, ...)은 분리된 값들을 정수로 변환합니다.
세 번째 줄은 M = int(input())으로, 찾아야 할 수의 개수를 읽어와 변수 M에 저장합니다.
네 번째 줄은 X = list(map(int, input().split()))으로, M개의 수들을 입력받아 리스트 X에 저장합니다. 위와 동일하게 입력을 공백을 기준으로 분리하고 정수로 변환합니다.


# 주어진 M개의 수들이 목록 A 안에 있는지 확인하기
for num in X:
    if num in A:
        print(1)
    else:
        print(0)
        
for num in X:에서 X 리스트의 각 요소에 대해 반복합니다.
if num in A:는 현재 순회 중인 num이 A 리스트에 있는지를 확인합니다.
num이 A 리스트에 있다면, print(1)로 해당 수가 존재함을 표시합니다.
num이 A 리스트에 없다면, print(0)으로 해당 수가 존재하지 않음을 표시합니다.
따라서 이 코드는 입력으로 주어진 수들이 목록 A 안에 있는지를 확인하고, 결과를 출력하는 코드입니다.


'''




# 